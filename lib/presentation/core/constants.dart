import 'package:flutter/material.dart';

const flushBarDuration = Duration(seconds: 4);

const commonPadding = 8.0;

const todosSpasing = 8.0;

final savingInProgressOverlayColor = Colors.black.withOpacity(0.8);
const circularProgressIndicatorColor = Colors.blue;
const circularProgressIndicatorDuration = Duration(milliseconds: 150);
